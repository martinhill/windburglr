TODO

1. scraper auto-reconnect to db
2. settings popup panel
    - slider for num minutes in current conditions
    - adjust auto-fetch interval
3. auto-fetch new data on interval, or manual fetch button
4. APScheduler, and deleting of old rows to manage db size

