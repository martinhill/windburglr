TODO

- APSchedulerand deleting of old rows to manage db size
- Upgrade to python 3.4
- Add primary key serial column to db table
- syslog
- authentication for unsafe API operations

- Implement proper (Atom?) feeds with caching
- auto-fetch new data on interval
- settings popup panel
    - slider for num minutes in current conditions
    - adjust auto-fetch interval
- Unify getdb() and connect_db() functions and place in common code module
- replace urllib2 with requests

